{
  "name": "cw-app-rebuild",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check:legacy-free": "node scripts/check-legacy-free.mjs"
  }
}
